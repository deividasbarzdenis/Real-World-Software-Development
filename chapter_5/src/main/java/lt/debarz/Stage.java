package lt.debarz;

/**
 * They may record on their
 * Customer Relationship Management (CRM) system different deals with different
 * amounts that have different stages. A deal stage may be represented as an enum
 * Stage with values
 * */
public enum Stage {
    LEAD, INTERESTED, EVALUATING, CLOSED
}
